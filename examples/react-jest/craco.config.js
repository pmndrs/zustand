const path = require('path')

module.exports = {
  webpack: {
    alias: {
      // zustand: path.resolve(__dirname, '../../dist'),
      // 'zustand/testing': path.resolve(__dirname, '../../dist/testing'),
      // 'zustand/vanilla': path.resolve(__dirname, '../../dist/vanilla'),
    }
  }
}
